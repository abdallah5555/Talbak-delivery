import { useEffect, useMemo, useState } from "react";
import { supabase } from "./lib/supabase";

type Store={id:string;name:string;category:string;description:string;image_url:string|null;phone:string|null;address:string;latitude:number|null;longitude:number|null;delivery_fee:number;min_order:number;is_open:boolean;prep_minutes:number;rating:number};
type Item={id:string;store_id:string;name:string;description:string;image_url:string|null;price:number;category:string;is_available:boolean};
type Line={item:Item;qty:number};
type Address={id:string;label:string;address_line:string;latitude:number|null;longitude:number|null;is_default:boolean};
type Order={id:string;store_id:string;customer_id:string;driver_id:string|null;status:string;subtotal:number;delivery_fee:number;total:number;payment_method:string;delivery_address:string;customer_note:string;estimated_minutes:number|null;created_at:string;updated_at:string;stores?:{name:string}|null};

const CATS=["الكل","مطاعم","بقالة","مخبوزات","صيدلية","مشروبات","حلويات","عناية","خدمات"];
const ICONS:Record<string,string>={"الكل":"✨","مطاعم":"🍔","بقالة":"🛒","مخبوزات":"🥐","صيدلية":"💊","مشروبات":"🥤","حلويات":"🍰","عناية":"🧴","خدمات":"📦"};
const STATUS:Record<string,string>={pending:"بنأكد الطلب",accepted:"المتجر استلم",preparing:"بيجهز طلبك",ready:"جاهز للسائق",assigned:"السائق استلمه",picked_up:"اتستلم من المتجر",on_the_way:"في الطريق ليك",delivered:"اتسلّم بنجاح",cancelled:"اتلغى",rejected:"المتجر رفض الطلب"};
const money=(n:number)=>`${new Intl.NumberFormat("ar-EG").format(Math.round(Number(n)||0))} ج.م`;
const since=(s:string)=>{const m=Math.max(0,Math.floor((Date.now()-new Date(s).getTime())/60000));if(m<1)return "دلوقتي";if(m<60)return `من ${m} د`;const h=Math.floor(m/60);return h<24?`من ${h} س`:`من ${Math.floor(h/24)} يوم`};

const demoStores:Store[]=[
{id:"demo-1",name:"برجر هب",category:"مطاعم",description:"برجر طازة • وجبات كومبو • تحضير سريع",image_url:"https://images.unsplash.com/photo-1568901346375-23c9450c58cd?auto=format&fit=crop&w=1200&q=80",phone:null,address:"مدينة نصر",latitude:null,longitude:null,delivery_fee:15,min_order:0,is_open:true,prep_minutes:18,rating:4.8},
{id:"demo-2",name:"سوبر ماركت النخبة",category:"بقالة",description:"كل احتياجات البيت في طلب واحد",image_url:"https://images.unsplash.com/photo-1542838132-92c53300491e?auto=format&fit=crop&w=1200&q=80",phone:null,address:"المعادي",latitude:null,longitude:null,delivery_fee:20,min_order:0,is_open:true,prep_minutes:10,rating:4.7},
{id:"demo-3",name:"بريد & بريك",category:"مخبوزات",description:"مخبوزات وفطار وحلويات يومية",image_url:"https://images.unsplash.com/photo-1509440159596-0249088772ff?auto=format&fit=crop&w=1200&q=80",phone:null,address:"التجمع",latitude:null,longitude:null,delivery_fee:12,min_order:0,is_open:true,prep_minutes:14,rating:4.9},
{id:"demo-4",name:"ركن القهوة",category:"مشروبات",description:"قهوة مختصة ومشروبات باردة",image_url:"https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?auto=format&fit=crop&w=1200&q=80",phone:null,address:"مصر الجديدة",latitude:null,longitude:null,delivery_fee:12,min_order:0,is_open:true,prep_minutes:12,rating:4.6},
{id:"demo-5",name:"صيدلية الحياة",category:"صيدلية",description:"مستلزمات يومية ومنتجات عناية",image_url:"https://images.unsplash.com/photo-1585435557343-3b092031a831?auto=format&fit=crop&w=1200&q=80",phone:null,address:"الرحاب",latitude:null,longitude:null,delivery_fee:18,min_order:0,is_open:true,prep_minutes:8,rating:4.8},
{id:"demo-6",name:"سكر زيادة",category:"حلويات",description:"كيك • كوكيز • حلويات للمناسبات",image_url:"https://images.unsplash.com/photo-1551024506-0bccd828d307?auto=format&fit=crop&w=1200&q=80",phone:null,address:"مصر الجديدة",latitude:null,longitude:null,delivery_fee:16,min_order:0,is_open:true,prep_minutes:20,rating:4.9}
];
const demoItems:Item[]=[
{id:"i1",store_id:"demo-1",name:"سماش برجر كلاسيك",description:"قطعتين لحم • جبنة • صوص البيت",image_url:"https://images.unsplash.com/photo-1571091718767-18b5b1457add?auto=format&fit=crop&w=700&q=80",price:145,category:"برجر",is_available:true},
{id:"i2",store_id:"demo-1",name:"تشيكن كرسبي",description:"دجاج مقرمش • جبنة • صوص",image_url:"https://images.unsplash.com/photo-1606755962773-d324e0a13086?auto=format&fit=crop&w=700&q=80",price:125,category:"برجر",is_available:true},
{id:"i3",store_id:"demo-1",name:"بطاطس تشيدر",description:"بطاطس مقرمشة مع تشيدر",image_url:"https://images.unsplash.com/photo-1630384060421-cb20d0e0649d?auto=format&fit=crop&w=700&q=80",price:75,category:"جانبي",is_available:true},
{id:"i4",store_id:"demo-2",name:"باكدج أسبوع البيت",description:"اختيارات أساسية موفرة",image_url:"https://images.unsplash.com/photo-1583258292688-d0213dc5a3a8?auto=format&fit=crop&w=700&q=80",price:420,category:"عروض",is_available:true},
{id:"i5",store_id:"demo-3",name:"كرواسون زبدة",description:"مخبوز طازة يوميًا",image_url:"https://images.unsplash.com/photo-1555507036-ab1f4038808a?auto=format&fit=crop&w=700&q=80",price:55,category:"مخبوزات",is_available:true},
{id:"i6",store_id:"demo-4",name:"آيس لاتيه",description:"إسبريسو • حليب • ثلج",image_url:"https://images.unsplash.com/photo-1517701604599-bb29b565090c?auto=format&fit=crop&w=700&q=80",price:85,category:"قهوة",is_available:true},
{id:"i7",store_id:"demo-5",name:"مجموعة عناية يومية",description:"غسول + مرطب + واقي شمس",image_url:"https://images.unsplash.com/photo-1556228578-8c89e6adf883?auto=format&fit=crop&w=700&q=80",price:295,category:"عناية",is_available:true},
{id:"i8",store_id:"demo-6",name:"كوكيز شوكولاتة",description:"كوكيز طرية محشية شوكولاتة",image_url:"https://images.unsplash.com/photo-1499636136210-6f4ee915583e?auto=format&fit=crop&w=700&q=80",price:85,category:"حلويات",is_available:true}
];

export default function App(){
 const [tab,setTab]=useState("home"),[stores,setStores]=useState<Store[]>(demoStores),[items,setItems]=useState<Item[]>(demoItems),[query,setQuery]=useState(""),[cat,setCat]=useState("الكل"),[active,setActive]=useState<Store|null>(null),[cart,setCart]=useState<Line[]>([]),[cartOpen,setCartOpen]=useState(false),[checkout,setCheckout]=useState(false),[authOpen,setAuthOpen]=useState(false),[authMode,setAuthMode]=useState<"login"|"signup">("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[name,setName]=useState(""),[phone,setPhone]=useState(""),[session,setSession]=useState<any>(null),[profile,setProfile]=useState<any>(null),[orders,setOrders]=useState<Order[]>([]),[addresses,setAddresses]=useState<Address[]>([]),[favorites,setFavorites]=useState<string[]>([]),[notifications,setNotifications]=useState<any[]>([]),[orderView,setOrderView]=useState<Order|null>(null),[review,setReview]=useState({rating:5,comment:""}),[reviewing,setReviewing]=useState<Order|null>(null),[newAddress,setNewAddress]=useState({label:"البيت",address_line:""}),[applyOpen,setApplyOpen]=useState<"merchant"|"driver"|null>(null),[apply,setApply]=useState({business_name:"",category:"مطاعم",address:"",phone:"",vehicle_type:"موتوسيكل",full_name:""}),[toast,setToast]=useState<string|null>(null),[addressId,setAddressId]=useState<string|null>(null),[note,setNote]=useState(""),[payment,setPayment]=useState("cash");
 const [loading,setLoading]=useState(true);
 useEffect(()=>{let alive=true;supabase.auth.getSession().then(({data})=>{if(alive){setSession(data.session);if(data.session)void loadUser(data.session.user.id);else setLoading(false)}});const {data}=supabase.auth.onAuthStateChange((_e,s)=>{setSession(s);if(s)void loadUser(s.user.id);else setLoading(false)});return()=>{alive=false;data.subscription.unsubscribe()}},[]);
 useEffect(()=>{if(!session)return;const uid=session.user.id;const channel=supabase.channel(`customer-${uid}`).on("postgres_changes",{event:"*",schema:"public",table:"orders",filter:`customer_id=eq.${uid}`},()=>void refreshOrders(uid)).on("postgres_changes",{event:"*",schema:"public",table:"notifications",filter:`user_id=eq.${uid}`},()=>void refreshNotifications(uid)).subscribe();return()=>{void supabase.removeChannel(channel)}},[session]);
 useEffect(()=>{if(toast){const t=window.setTimeout(()=>setToast(null),2400);return()=>window.clearTimeout(t)}},[toast]);
 async function loadUser(uid:string){setLoading(true);const [p,o,a,f,n,s,m]=await Promise.all([supabase.from("profiles").select("*").eq("id",uid).maybeSingle(),supabase.from("orders").select("*,stores(name)").eq("customer_id",uid).order("created_at",{ascending:false}).limit(60),supabase.from("addresses").select("*").eq("user_id",uid).order("is_default",{ascending:false}).order("created_at",{ascending:false}),supabase.from("favorites").select("store_id").eq("user_id",uid),supabase.from("notifications").select("*").eq("user_id",uid).order("created_at",{ascending:false}).limit(30),supabase.from("stores").select("*").order("rating",{ascending:false}),supabase.from("menu_items").select("*").eq("is_available",true)]);if(p.data)setProfile(p.data);if(o.data)setOrders(o.data);if(a.data)setAddresses(a.data);if(f.data)setFavorites(f.data.map((x:any)=>x.store_id));if(n.data)setNotifications(n.data);if(s.data?.length)setStores(s.data);if(m.data?.length)setItems(m.data);setLoading(false)}
 async function refreshOrders(uid=session?.user?.id){if(!uid)return;const {data}=await supabase.from("orders").select("*,stores(name)").eq("customer_id",uid).order("created_at",{ascending:false}).limit(60);if(data)setOrders(data)}
 async function refreshNotifications(uid=session?.user?.id){if(!uid)return;const {data}=await supabase.from("notifications").select("*").eq("user_id",uid).order("created_at",{ascending:false}).limit(30);if(data)setNotifications(data)}
 const filtered=useMemo(()=>stores.filter(s=>(cat==="الكل"||s.category===cat)&&(!query||`${s.name} ${s.description} ${s.category}`.toLowerCase().includes(query.toLowerCase()))),[stores,cat,query]);
 const menu=useMemo(()=>active?items.filter(i=>i.store_id===active.id&&i.is_available):[],[items,active]);
 const cartCount=cart.reduce((a,l)=>a+l.qty,0),subtotal=cart.reduce((a,l)=>a+l.qty*Number(l.item.price),0),cartStore=cart[0]?stores.find(s=>s.id===cart[0].item.store_id):null,delivery=Number(cartStore?.delivery_fee||0),total=subtotal+delivery,unread=notifications.filter(n=>!n.is_read).length;
 const selectedAddress=addresses.find(a=>a.id===addressId)||addresses.find(a=>a.is_default);
 const track=(s:string)=>["pending","accepted","preparing","ready","assigned","picked_up","on_the_way","delivered"].indexOf(s);
 function add(item:Item){if(!cartStore&&active&&!active.is_open){setToast("المتجر مقفول دلوقتي");return}if(cart.length&&cart[0].item.store_id!==item.store_id){setToast("السلة لمتجر واحد — فضّي السلة الأول");return}setCart(xs=>{const f=xs.find(x=>x.item.id===item.id);return f?xs.map(x=>x.item.id===item.id?{...x,qty:x.qty+1}:x):[...xs,{item,qty:1}]});setToast("اتضاف للسلة ✓")}
 function changeQty(id:string,d:number){setCart(xs=>xs.map(x=>x.item.id===id?{...x,qty:x.qty+d}:x).filter(x=>x.qty>0))}
 async function authenticate(){if(!email||password.length<6){setToast("اكتب إيميل صحيح وكلمة سر 6 أحرف على الأقل");return}const meta={data:{full_name:name||"مستخدم طلبك",phone:phone||null}};const r=authMode==="login"?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{data:meta.data}});if(r.error){setToast(r.error.message);return}setAuthOpen(false);setToast(authMode==="login"?"أهلاً بيك 👋":"تم إنشاء الحساب، راجع بريدك لو التأكيد مطلوب")}
 async function placeOrder(){if(!session){setAuthOpen(true);setToast("سجّل دخول الأول");return}if(!cart.length||!cartStore){return}const addr=selectedAddress?.address_line||"";if(!addr.trim()){setToast("أضف عنوان توصيل الأول");setTab("profile");return}const payload=cart.map(l=>({menu_item_id:l.item.id,quantity:l.qty}));const {data,error}=await supabase.rpc("create_order_secure",{p_store_id:cartStore.id,p_items:payload,p_address:addr,p_payment_method:payment,p_note:note});if(error){setToast(error.message);return}setOrders(xs=>[{...data,stores:{name:cartStore.name}},...xs]);setCart([]);setCheckout(false);setCartOpen(false);setTab("orders");setToast("طلبك اتسجل بنجاح 🎉")}
 async function saveAddress(){if(!session||!newAddress.address_line.trim()){setToast("اكتب العنوان");return}if(addresses.length===0){const {data,error}=await supabase.from("addresses").insert({user_id:session.user.id,label:newAddress.label,address_line:newAddress.address_line.trim(),is_default:true}).select().single();if(error)return setToast(error.message);setAddresses([data]);setAddressId(data.id);return}const {data,error}=await supabase.from("addresses").insert({user_id:session.user.id,label:newAddress.label,address_line:newAddress.address_line.trim(),is_default:false}).select().single();if(error)return setToast(error.message);setAddresses(xs=>[...xs,data]);setNewAddress({label:"البيت",address_line:""})}
 async function makeDefault(a:Address){if(!session)return;await supabase.from("addresses").update({is_default:false}).eq("user_id",session.user.id);const {error}=await supabase.from("addresses").update({is_default:true}).eq("id",a.id).eq("user_id",session.user.id);if(error)return setToast(error.message);setAddresses(xs=>xs.map(x=>({...x,is_default:x.id===a.id})));setAddressId(a.id)}
 async function deleteAddress(id:string){if(!session)return;const {error}=await supabase.from("addresses").delete().eq("id",id).eq("user_id",session.user.id);if(error)return setToast(error.message);setAddresses(xs=>xs.filter(x=>x.id!==id));if(addressId===id)setAddressId(null)}
 async function toggleFavorite(id:string){if(!session){setAuthOpen(true);return}if(favorites.includes(id)){const {error}=await supabase.from("favorites").delete().eq("user_id",session.user.id).eq("store_id",id);if(error)return setToast(error.message);setFavorites(xs=>xs.filter(x=>x!==id))}else{const {error}=await supabase.from("favorites").insert({user_id:session.user.id,store_id:id});if(error)return setToast(error.message);setFavorites(xs=>[...xs,id])}}
 async function cancelOrder(o:Order){const {error}=await supabase.rpc("customer_cancel_order",{p_order_id:o.id});if(error)return setToast(error.message);setOrders(xs=>xs.map(x=>x.id===o.id?{...x,status:"cancelled"}:x));setToast("اتلغى الطلب")}
 async function submitReview(){if(!reviewing)return;const {error}=await supabase.rpc("submit_store_review",{p_order_id:reviewing.id,p_rating:review.rating,p_comment:review.comment});if(error)return setToast(error.message);setReviewing(null);setReview({rating:5,comment:""});setToast("شكرًا لتقييمك ❤️")}
 async function apply(){if(!session||!applyOpen)return;const r=applyOpen==="merchant"?await supabase.rpc("apply_as_merchant",{p_business_name:apply.business_name,p_phone:apply.phone,p_address:apply.address,p_category:apply.category}):await supabase.rpc("apply_as_driver",{p_full_name:apply.full_name,p_phone:apply.phone,p_vehicle_type:apply.vehicle_type});if(r.error){setToast(r.error.message);return}setApplyOpen(null);setToast("طلب الانضمام اتبعت للإدارة")}
 async function readNotifications(){if(!session||!unread)return;await supabase.from("notifications").update({is_read:true}).eq("user_id",session.user.id).eq("is_read",false);setNotifications(xs=>xs.map(x=>({...x,is_read:true})))}
 if(loading)return <div className="boot">جاري تشغيل طلبك…</div>;
 return <div className="app" dir="rtl">
 <header><div className="brand" onClick={()=>setTab("home")}><span className="logo">ط</span><div><b>طلبك</b><small>دليفري</small></div></div><div className="actions"><button onClick={()=>setTab("profile")}>⌖</button><button onClick={()=>{setTab("profile");void readNotifications()}}>🔔{unread>0&&<i>{unread}</i>}</button><button className="avatar" onClick={()=>setTab("profile")}>{(profile?.full_name||"ز").slice(0,1)}</button></div></header>
 {tab==="home"&&<main className="page"><section className="hero"><div><span className="kicker lime">طلبك في طريقه ليك</span><h1>الأكل، البيت، <em>وأي حاجة</em>… في طلب واحد.</h1><p>اكتشف اللي حواليك، اختار طلبك، وخلّي علينا باقي الرحلة.</p><div className="search"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="مطعم، أكلة، منتج…"/>{query&&<button onClick={()=>setQuery("")}>×</button>}</div><div className="trust"><span>⚡ سريع</span><span>🛡️ موثوق</span><span>💵 كاش</span></div></div><div className="hero-art"><div className="glow one"/><div className="glow two"/><div className="promo"><small>🔥 الأكثر طلبًا</small><b>سماش برجر + بطاطس</b><span>من 145 ج.م • تحضير سريع</span><div><i/></div></div></div></section>
 <section className="section"><div className="section-head"><div><span className="kicker">استكشف</span><h2>إيه نفسك فيه؟</h2></div><button onClick={()=>setCat("الكل")}>كل الأقسام</button></div><div className="chips">{CATS.map(c=><button key={c} className={cat===c?"active":""} onClick={()=>setCat(c)}><span>{ICONS[c]}</span>{c}</button>)}</div></section>
 <section className="section"><div className="section-head"><div><span className="kicker">مختارات قريبة</span><h2>{cat==="الكل"?"أماكن تستاهل التجربة":cat}</h2></div><small>{filtered.length} مكان</small></div><div className="grid">{filtered.map(s=><article key={s.id} className="store" onClick={()=>setActive(s)}><div className="media">{s.image_url?<img src={s.image_url} alt=""/>:<div className="fallback">{s.name[0]}</div>}<button className={`heart ${favorites.includes(s.id)?"on":""}`} onClick={e=>{e.stopPropagation();void toggleFavorite(s.id)}}>{favorites.includes(s.id)?"♥":"♡"}</button><span>{s.is_open?"مفتوح":"مغلق"}</span></div><div className="store-copy"><div><h3>{s.name}</h3><b>★ {Number(s.rating||0).toFixed(1)}</b></div><p>{s.description}</p><small>{s.category} <i>•</i> {s.prep_minutes+20}–{s.prep_minutes+30} د <i>•</i> {money(s.delivery_fee)} توصيل</small></div></article>)}</div></section>
 <section className="partner"><div><span className="kicker">لأصحاب المتاجر والسائقين</span><h2>كبر شغلك مع طلبك.</h2><p>استقبل الطلبات، اشتغل أونلاين، وخلّي المنصة تجمع لك التشغيل في مكان واحد.</p><div className="partner-actions"><button onClick={()=>setApplyOpen("merchant")}>أنا متجر</button><button className="ghost" onClick={()=>setApplyOpen("driver")}>أنا سائق</button></div></div><div className="numbers"><div><b>24/7</b><span>تشغيل</span></div><div><b>مكان واحد</b><span>لكل الطلبات</span></div></div></section></main>}
 {tab==="orders"&&<main className="page inner"><div className="title"><div><span className="kicker">رحلة طلباتك</span><h1>طلباتي</h1></div><button className="outline" onClick={()=>setTab("home")}>+ طلب جديد</button></div>{!session?<div className="login-cta"><div><b>سجّل دخول عشان تحفظ الرحلات</b><p>طلباتك، المفضلة، العناوين والإشعارات في مكان واحد.</p></div><button onClick={()=>setAuthOpen(true)}>تسجيل الدخول</button></div>:orders.length?orders.map(o=><article className="order" key={o.id}><div className="order-head"><div><small>#{o.id.slice(0,6).toUpperCase()} • {since(o.created_at)}</small><h3>{o.stores?.name||stores.find(s=>s.id===o.store_id)?.name||"متجر"}</h3></div><span>{STATUS[o.status]||o.status}</span></div><div className="steps">{["استلام","تجهيز","سائق","طريق","تم"].map((x,i)=><div className={track(o.status)>=i*2?"done":""} key={x}><i>{i===4?"✓":"•"}</i><small>{x}</small></div>)}</div><div className="order-foot"><span>{o.delivery_address}</span><b>{money(Number(o.total))}</b><button onClick={()=>setOrderView(o)}>تفاصيل</button>{["pending","accepted"].includes(o.status)&&<button className="danger-link" onClick={()=>void cancelOrder(o)}>إلغاء</button>}{o.status==="delivered"&&<button onClick={()=>setReviewing(o)}>قيّم</button>}</div></article>):<div className="empty"><div>🛍️</div><h3>لسه مفيش طلبات</h3><p>ابدأ أول طلب وهتلاقي الرحلة كلها هنا.</p><button onClick={()=>setTab("home")}>ابدأ دلوقتي</button></div>}</main>}
 {tab==="saved"&&<main className="page inner"><div className="title"><div><span className="kicker">محفوظاتك</span><h1>المفضلة</h1></div></div>{favorites.length?<div className="grid">{stores.filter(s=>favorites.includes(s.id)).map(s=><article key={s.id} className="store" onClick={()=>setActive(s)}><div className="media">{s.image_url&&<img src={s.image_url} alt=""/>}<button className="heart on" onClick={e=>{e.stopPropagation();void toggleFavorite(s.id)}}>♥</button></div><div className="store-copy"><div><h3>{s.name}</h3><b>★ {Number(s.rating||0).toFixed(1)}</b></div><p>{s.description}</p><small>{s.category} <i>•</i> {money(s.delivery_fee)} توصيل</small></div></article>)}</div>:<div className="empty"><div>♡</div><h3>المفضلة فاضية</h3><p>اضغط القلب على أي متجر عشان تحفظه.</p><button onClick={()=>setTab("home")}>استكشف</button></div>}</main>}
 {tab==="profile"&&<main className="page inner"><section className="profile"><div className="pavatar">{(profile?.full_name||"ز").slice(0,1)}</div><div><span className="kicker lime">حسابك</span><h1>{session?(profile?.full_name||"مستخدم طلبك"):"ضيف"}</h1><p>{session?(profile?.phone||session.user.email):"سجّل الدخول عشان كل المميزات"}</p></div>{session?<button onClick={()=>{void supabase.auth.signOut()}}>خروج</button>:<button onClick={()=>setAuthOpen(true)}>تسجيل الدخول</button>}</section>
 <section className="notifications"><div className="section-head"><div><span className="kicker">حافظ على تنظيمك</span><h2>الإشعارات</h2></div></div>{notifications.length?notifications.slice(0,5).map(n=><div className={`note ${n.is_read?"":"new"}`} key={n.id}><span>●</span><div><b>{n.title}</b><p>{n.body}</p><small>{since(n.created_at)}</small></div></div>):<div className="muted">مفيش إشعارات جديدة.</div>}</section>
 <section className="section"><div className="section-head"><div><span className="kicker">التوصيل</span><h2>عناويني</h2></div></div><div className="address-list">{addresses.map(a=><div className={`address-card ${a.is_default?"default":""}`} key={a.id}><div><b>{a.label}</b><p>{a.address_line}</p>{a.is_default&&<small>العنوان الافتراضي</small>}</div><div className="address-actions">{!a.is_default&&<button onClick={()=>void makeDefault(a)}>اختيار</button>}<button onClick={()=>void deleteAddress(a.id)}>حذف</button></div></div>)}<div className="add-address"><input value={newAddress.label} onChange={e=>setNewAddress({...newAddress,label:e.target.value})} placeholder="اسم العنوان"/><input value={newAddress.address_line} onChange={e=>setNewAddress({...newAddress,address_line:e.target.value})} placeholder="العنوان بالتفصيل"/><button className="primary" onClick={()=>void saveAddress()}>+ إضافة عنوان</button></div></div></section>
 <section className="section"><div className="section-head"><div><span className="kicker">مع طلبك</span><h2>اشتغل معانا</h2></div></div><div className="join-grid"><button onClick={()=>setApplyOpen("merchant")}><span>🏪</span><b>انضم كتاجر</b><small>اعرض منتجاتك واستقبل الطلبات</small></button><button onClick={()=>setApplyOpen("driver")}><span>🛵</span><b>اشتغل كسائق</b><small>كن أونلاين واستقبل رحلات</small></button></div></section></main>}
 <nav className="nav"><button className={tab==="home"?"active":""} onClick={()=>setTab("home")}><b>⌂</b>الرئيسية</button><button className={tab==="orders"?"active":""} onClick={()=>setTab("orders")}><b>▣</b>طلباتي</button><button className="cart" onClick={()=>setCartOpen(true)}>🛒{cartCount>0&&<i>{cartCount}</i>}</button><button className={tab==="saved"?"active":""} onClick={()=>setTab("saved")}><b>♡</b>المفضلة</button><button className={tab==="profile"?"active":""} onClick={()=>setTab("profile")}><b>◯</b>حسابي</button></nav>
 {active&&<div className="shade" onClick={()=>setActive(null)}><div className="store-modal" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setActive(null)}>×</button><div className="cover">{active.image_url&&<img src={active.image_url} alt=""/>}</div><div className="modal-body"><div className="modal-title"><div><span className="open">{active.is_open?"مفتوح الآن":"مغلق الآن"}</span><h2>{active.name}</h2><p>{active.description}</p></div><button className={`heart big ${favorites.includes(active.id)?"on":""}`} onClick={()=>void toggleFavorite(active.id)}>{favorites.includes(active.id)?"♥":"♡"}</button></div><div className="meta">★ {Number(active.rating||0).toFixed(1)} <i>•</i> {active.prep_minutes+20}–{active.prep_minutes+30} دقيقة <i>•</i> {money(active.delivery_fee)} توصيل</div><div className="menu">{menu.length?menu.map(it=><div className="item" key={it.id}><div className="item-img">{it.image_url&&<img src={it.image_url} alt=""/>}</div><div><b>{it.name}</b><p>{it.description}</p><strong>{money(Number(it.price))}</strong></div><button onClick={()=>add(it)}>+</button></div>):<div className="muted">الأصناف هتظهر هنا لما تتضاف.</div>}</div></div></div></div>}
 {cartOpen&&<div className="shade" onClick={()=>setCartOpen(false)}><aside className="drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><div><span className="kicker">جاهز؟</span><h2>سلتك</h2></div><button className="x" onClick={()=>setCartOpen(false)}>×</button></div>{cart.length?<><div className="cart-store"><b>{cartStore?.name}</b><small>{cartCount} منتج</small></div>{cart.map(l=><div className="line" key={l.item.id}><img src={l.item.image_url||""} alt=""/><div><b>{l.item.name}</b><small>{money(Number(l.item.price))}</small></div><div className="counter"><button onClick={()=>changeQty(l.item.id,-1)}>−</button><b>{l.qty}</b><button onClick={()=>changeQty(l.item.id,1)}>+</button></div></div>)}<div className="sum"><div><span>الإجمالي الفرعي</span><b>{money(subtotal)}</b></div><div><span>التوصيل</span><b>{money(delivery)}</b></div><div className="grand"><span>الإجمالي</span><b>{money(total)}</b></div></div><button className="primary full" onClick={()=>setCheckout(true)}>إتمام الطلب</button></>:<div className="empty"><div>🛒</div><h3>السلة فاضية</h3><p>ضيف أول منتج عشان تبدأ.</p></div>}{checkout&&<div className="checkout"><div className="drawer-head"><div><span className="kicker">آخر خطوة</span><h2>تأكيد الطلب</h2></div><button className="x" onClick={()=>setCheckout(false)}>×</button></div>{addresses.length?<div className="checkout-addresses">{addresses.map(a=><button key={a.id} className={selectedAddress?.id===a.id?"selected":""} onClick={()=>setAddressId(a.id)}><b>{a.label}</b><span>{a.address_line}</span></button>)}</div>:<div className="login-cta"><div><b>مفيش عنوان محفوظ</b><p>أضف عنوان من صفحة حسابك.</p></div><button onClick={()=>{setCheckout(false);setCartOpen(false);setTab("profile")}}>إضافة</button></div>}<label>ملاحظات للطلب<textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="الدور، علامة مميزة، تعليمات للسائق..."/></label><div className="payments"><button className={payment==="cash"?"selected":""} onClick={()=>setPayment("cash")}>💵<b>كاش</b><small>عند الاستلام</small></button><button className={payment==="online"?"selected":""} onClick={()=>setPayment("online")}>💳<b>إلكتروني</b><small>جاهز للربط</small></button></div><button className="primary full" onClick={()=>void placeOrder()}>تأكيد الطلب • {money(total)}</button></div>}</aside></div>}
 {authOpen&&<div className="shade" onClick={()=>setAuthOpen(false)}><div className="auth" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setAuthOpen(false)}>×</button><span className="auth-logo">ط</span><span className="kicker">{authMode==="login"?"رجوعك يسعدنا":"أهلاً بيك"}</span><h2>{authMode==="login"?"سجّل دخولك":"اعمل حساب جديد"}</h2><p>{authMode==="login"?"خلي طلباتك ومفضلاتك معاك دايمًا.":"حسابك هيفتح لك كل مميزات طلبك."}</p>{authMode==="signup"&&<><input value={name} onChange={e=>setName(e.target.value)} placeholder="الاسم بالكامل"/><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="رقم الموبايل"/></>}<input value={email} onChange={e=>setEmail(e.target.value)} placeholder="البريد الإلكتروني"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="كلمة السر"/><button className="primary full" onClick={()=>void authenticate()}>{authMode==="login"?"دخول":"إنشاء الحساب"}</button><button className="switch" onClick={()=>setAuthMode(authMode==="login"?"signup":"login")}>{authMode==="login"?"لسه معندكش حساب؟ اعمل حساب":"عندك حساب بالفعل؟ سجّل دخول"}</button></div></div>}
 {orderView&&<div className="shade" onClick={()=>setOrderView(null)}><div className="order-detail" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setOrderView(null)}>×</button><span className="kicker">متابعة لحظية</span><h2>{orderView.stores?.name||"طلبك"}</h2><div className="detail-status"><b>{STATUS[orderView.status]||orderView.status}</b><small>آخر تحديث {since(orderView.updated_at)}</small></div><div className="detail-total"><span>الإجمالي</span><b>{money(Number(orderView.total))}</b></div><div className="detail-row"><span>العنوان</span><b>{orderView.delivery_address}</b></div><div className="detail-row"><span>طريقة الدفع</span><b>{orderView.payment_method==="cash"?"كاش عند الاستلام":"إلكتروني"}</b></div>{orderView.estimated_minutes&&<div className="eta">⏱️ متوقع خلال حوالي {orderView.estimated_minutes} دقيقة</div>}</div></div>}
 {reviewing&&<div className="shade" onClick={()=>setReviewing(null)}><div className="auth" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setReviewing(null)}>×</button><span className="kicker">رأيك يفرق</span><h2>قيّم تجربتك</h2><div className="stars">{[1,2,3,4,5].map(n=><button key={n} className={n<=review.rating?"on":""} onClick={()=>setReview({...review,rating:n})}>★</button>)}</div><textarea className="review-box" value={review.comment} onChange={e=>setReview({...review,comment:e.target.value})} placeholder="قول لنا رأيك..."/><button className="primary full" onClick={()=>void submitReview()}>إرسال التقييم</button></div></div>}
 {applyOpen&&<div className="shade" onClick={()=>setApplyOpen(null)}><div className="auth" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setApplyOpen(null)}>×</button><span className="kicker">انضم لطلبك</span><h2>{applyOpen==="merchant"?"طلب انضمام متجر":"طلب انضمام سائق"}</h2>{applyOpen==="merchant"?<><input value={apply.business_name} onChange={e=>setApply({...apply,business_name:e.target.value})} placeholder="اسم النشاط"/><input value={apply.phone} onChange={e=>setApply({...apply,phone:e.target.value})} placeholder="رقم الموبايل"/><input value={apply.address} onChange={e=>setApply({...apply,address:e.target.value})} placeholder="عنوان النشاط"/><input value={apply.category} onChange={e=>setApply({...apply,category:e.target.value})} placeholder="التصنيف"/></>:<><input value={apply.full_name} onChange={e=>setApply({...apply,full_name:e.target.value})} placeholder="الاسم بالكامل"/><input value={apply.phone} onChange={e=>setApply({...apply,phone:e.target.value})} placeholder="رقم الموبايل"/><input value={apply.vehicle_type} onChange={e=>setApply({...apply,vehicle_type:e.target.value})} placeholder="نوع المركبة"/></>}<button className="primary full" onClick={()=>void apply()}>إرسال الطلب</button></div></div>}
 {toast&&<div className="toast">{toast}</div>}
 </div>
}
