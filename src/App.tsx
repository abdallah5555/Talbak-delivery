import { useEffect,useMemo,useState } from "react";
import { supabase } from "./lib/supabase";

type Store={id:string;name:string;category:string;description:string;image_url:string|null;address:string;delivery_fee:number;prep_minutes:number;rating:number;is_open:boolean};
type Item={id:string;store_id:string;name:string;description:string;image_url:string|null;price:number;category:string;is_available:boolean};
type Line={item:Item;qty:number};
type Address={id:string;label:string;address_line:string;is_default:boolean};
type Order={id:string;store_id:string;status:string;total:number;subtotal:number;delivery_fee:number;payment_method:string;delivery_address:string;created_at:string;updated_at:string;estimated_minutes:number|null;stores?:{name:string}|null};
const CATS=["الكل","مطاعم","بقالة","مخبوزات","صيدلية","مشروبات","حلويات","عناية","خدمات"];
const IC:Record<string,string>={"الكل":"✨","مطاعم":"🍔","بقالة":"🛒","مخبوزات":"🥐","صيدلية":"💊","مشروبات":"🥤","حلويات":"🍰","عناية":"🧴","خدمات":"📦"};
const ST:Record<string,string>={pending:"بنأكد الطلب",accepted:"المتجر استلم",preparing:"بيجهز طلبك",ready:"جاهز للسائق",assigned:"السائق استلمه",picked_up:"اتستلم من المتجر",on_the_way:"في الطريق ليك",delivered:"اتسلّم بنجاح",cancelled:"اتلغى",rejected:"مرفوض"};
const money=(n:number)=>`${new Intl.NumberFormat("ar-EG").format(Math.round(Number(n)||0))} ج.م`;
const ago=(s:string)=>{const m=Math.max(0,Math.floor((Date.now()-new Date(s).getTime())/60000));return m<1?"دلوقتي":m<60?`من ${m} د`:m<1440?`من ${Math.floor(m/60)} س`:`من ${Math.floor(m/1440)} يوم`};
const demoStores:Store[]=[{id:"d1",name:"برجر هب",category:"مطاعم",description:"برجر طازة ووجبات كومبو بتحضير سريع",image_url:"https://images.unsplash.com/photo-1568901346375-23c9450c58cd?auto=format&fit=crop&w=1200&q=80",address:"مدينة نصر",delivery_fee:15,prep_minutes:18,rating:4.8,is_open:true},{id:"d2",name:"سوبر ماركت النخبة",category:"بقالة",description:"احتياجات البيت في طلب واحد",image_url:"https://images.unsplash.com/photo-1542838132-92c53300491e?auto=format&fit=crop&w=1200&q=80",address:"المعادي",delivery_fee:20,prep_minutes:10,rating:4.7,is_open:true},{id:"d3",name:"بريد & بريك",category:"مخبوزات",description:"مخبوزات وفطار وحلويات يومية",image_url:"https://images.unsplash.com/photo-1509440159596-0249088772ff?auto=format&fit=crop&w=1200&q=80",address:"التجمع",delivery_fee:12,prep_minutes:14,rating:4.9,is_open:true},{id:"d4",name:"ركن القهوة",category:"مشروبات",description:"قهوة ومشروبات باردة",image_url:"https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?auto=format&fit=crop&w=1200&q=80",address:"مصر الجديدة",delivery_fee:12,prep_minutes:12,rating:4.6,is_open:true},{id:"d5",name:"صيدلية الحياة",category:"صيدلية",description:"مستلزمات وعناية يومية",image_url:"https://images.unsplash.com/photo-1585435557343-3b092031a831?auto=format&fit=crop&w=1200&q=80",address:"الرحاب",delivery_fee:18,prep_minutes:8,rating:4.8,is_open:true},{id:"d6",name:"سكر زيادة",category:"حلويات",description:"كيك وكوكيز وحلويات للمناسبات",image_url:"https://images.unsplash.com/photo-1551024506-0bccd828d307?auto=format&fit=crop&w=1200&q=80",address:"مصر الجديدة",delivery_fee:16,prep_minutes:20,rating:4.9,is_open:true}];
const demoItems:Item[]=[{id:"i1",store_id:"d1",name:"سماش برجر كلاسيك",description:"قطعتين لحم • جبنة • صوص البيت",image_url:"https://images.unsplash.com/photo-1571091718767-18b5b1457add?auto=format&fit=crop&w=700&q=80",price:145,category:"برجر",is_available:true},{id:"i2",store_id:"d1",name:"تشيكن كرسبي",description:"دجاج مقرمش • جبنة • صوص",image_url:"https://images.unsplash.com/photo-1606755962773-d324e0a13086?auto=format&fit=crop&w=700&q=80",price:125,category:"برجر",is_available:true},{id:"i3",store_id:"d1",name:"بطاطس تشيدر",description:"بطاطس مقرمشة مع تشيدر",image_url:"https://images.unsplash.com/photo-1630384060421-cb20d0e0649d?auto=format&fit=crop&w=700&q=80",price:75,category:"جانبي",is_available:true},{id:"i4",store_id:"d2",name:"باكدج أسبوع البيت",description:"اختيارات أساسية موفرة",image_url:"https://images.unsplash.com/photo-1583258292688-d0213dc5a3a8?auto=format&fit=crop&w=700&q=80",price:420,category:"عروض",is_available:true},{id:"i5",store_id:"d3",name:"كرواسون زبدة",description:"مخبوز طازة يوميًا",image_url:"https://images.unsplash.com/photo-1555507036-ab1f4038808a?auto=format&fit=crop&w=700&q=80",price:55,category:"مخبوزات",is_available:true},{id:"i6",store_id:"d4",name:"آيس لاتيه",description:"إسبريسو • حليب • ثلج",image_url:"https://images.unsplash.com/photo-1517701604599-bb29b565090c?auto=format&fit=crop&w=700&q=80",price:85,category:"قهوة",is_available:true},{id:"i7",store_id:"d5",name:"مجموعة عناية يومية",description:"غسول + مرطب + واقي شمس",image_url:"https://images.unsplash.com/photo-1556228578-8c89e6adf883?auto=format&fit=crop&w=700&q=80",price:295,category:"عناية",is_available:true},{id:"i8",store_id:"d6",name:"كوكيز شوكولاتة",description:"كوكيز طرية محشية شوكولاتة",image_url:"https://images.unsplash.com/photo-1499636136210-6f4ee915583e?auto=format&fit=crop&w=700&q=80",price:85,category:"حلويات",is_available:true}];

export default function App(){
 const [tab,setTab]=useState("home"),[stores,setStores]=useState<Store[]>(demoStores),[items,setItems]=useState<Item[]>(demoItems),[query,setQuery]=useState(""),[cat,setCat]=useState("الكل"),[active,setActive]=useState<Store|null>(null),[cart,setCart]=useState<Line[]>([]),[cartOpen,setCartOpen]=useState(false),[checkout,setCheckout]=useState(false),[session,setSession]=useState<any>(null),[profile,setProfile]=useState<any>(null),[orders,setOrders]=useState<Order[]>([]),[addresses,setAddresses]=useState<Address[]>([]),[favorites,setFavorites]=useState<string[]>([]),[notifications,setNotifications]=useState<any[]>([]),[newAddress,setNewAddress]=useState({label:"البيت",address_line:""}),[addressId,setAddressId]=useState<string|null>(null),[note,setNote]=useState(""),[payment,setPayment]=useState<"cash"|"online">("cash"),[orderView,setOrderView]=useState<Order|null>(null),[reviewing,setReviewing]=useState<Order|null>(null),[rating,setRating]=useState(5),[comment,setComment]=useState(""),[joinMode,setJoinMode]=useState<"merchant"|"driver"|null>(null),[applicationForm,setApplicationForm]=useState({business_name:"",category:"مطاعم",custom_category:"",address:"",phone:"",full_name:"",vehicle_type:"موتسيكل"}),[toast,setToast]=useState<string|null>(null),[boot,setBoot]=useState(true);
 const openLogin=()=>{window.location.href="/?login=1"}; const openSignup=()=>{window.location.href="/?signup=1"};
 useEffect(()=>{let live=true;supabase.auth.getSession().then(({data})=>{if(!live)return;setSession(data.session);if(data.session)void load(data.session.user.id);else setBoot(false)});const {data}=supabase.auth.onAuthStateChange((_e,s)=>{setSession(s);if(s)void load(s.user.id);else setBoot(false)});return()=>{live=false;data.subscription.unsubscribe()}},[]);
 useEffect(()=>{if(!session)return;const uid=session.user.id;const ch=supabase.channel(`cust-${uid}`).on("postgres_changes",{event:"*",schema:"public",table:"orders",filter:`customer_id=eq.${uid}`},()=>void refreshOrders(uid)).on("postgres_changes",{event:"*",schema:"public",table:"notifications",filter:`user_id=eq.${uid}`},()=>void refreshNotifications(uid)).subscribe();return()=>{void supabase.removeChannel(ch)}},[session]);
 useEffect(()=>{if(!toast)return;const t=window.setTimeout(()=>setToast(null),2400);return()=>window.clearTimeout(t)},[toast]);
 async function load(uid:string){const [p,o,a,f,n,s,m]=await Promise.all([supabase.from("profiles").select("*").eq("id",uid).maybeSingle(),supabase.from("orders").select("*,stores(name)").eq("customer_id",uid).order("created_at",{ascending:false}).limit(60),supabase.from("addresses").select("*").eq("user_id",uid).order("is_default",{ascending:false}),supabase.from("favorites").select("store_id").eq("user_id",uid),supabase.from("notifications").select("*").eq("user_id",uid).order("created_at",{ascending:false}).limit(30),supabase.from("stores").select("*").order("rating",{ascending:false}),supabase.from("menu_items").select("*").eq("is_available",true)]);if(p.data)setProfile(p.data);if(o.data)setOrders(o.data);if(a.data){setAddresses(a.data);if(!addressId)setAddressId(a.data.find((x:any)=>x.is_default)?.id||a.data[0]?.id||null)}if(f.data)setFavorites(f.data.map((x:any)=>x.store_id));if(n.data)setNotifications(n.data);if(s.data?.length)setStores(s.data);if(m.data?.length)setItems(m.data);setBoot(false)}
 async function refreshOrders(uid:string){const {data}=await supabase.from("orders").select("*,stores(name)").eq("customer_id",uid).order("created_at",{ascending:false}).limit(60);if(data)setOrders(data)}
 async function refreshNotifications(uid:string){const {data}=await supabase.from("notifications").select("*").eq("user_id",uid).order("created_at",{ascending:false}).limit(30);if(data)setNotifications(data)}
 function addItem(item:Item){if(!item.is_available||!active?.is_open){setToast("الصنف أو المتجر غير متاح دلوقتي");return}if(cart.length&&cart[0].item.store_id!==item.store_id){setToast("السلة لمتجر واحد — فضّي السلة الأول");return}setCart(xs=>{const f=xs.find(x=>x.item.id===item.id);return f?xs.map(x=>x.item.id===item.id?{...x,qty:Math.min(30,x.qty+1)}:x):[...xs,{item,qty:1}]});setToast("اتضاف للسلة ✓")}
 function qty(id:string,d:number){setCart(xs=>xs.map(x=>x.item.id===id?{...x,qty:Math.max(0,Math.min(30,x.qty+d))}:x).filter(x=>x.qty>0))}
 async function place(){if(!session){openLogin();return}if(!cart.length)return;if(!selectedAddressId){setTab("profile");setCartOpen(false);return setToast("اختار أو أضف عنوان توصيل الأول")};const store=stores.find(s=>s.id===cart[0].item.store_id);if(!store)return;const {data,error}=await supabase.rpc("create_order_secure",{p_store_id:store.id,p_items:cart.map(x=>({menu_item_id:x.item.id,quantity:x.qty})),p_address:addresses.find(a=>a.id===selectedAddressId)?.address_line||"",p_payment_method:payment,p_note:note});if(error)return setToast(error.message);if(data)setOrders(xs=>[data,...xs]);setCart([]);setCheckout(false);setCartOpen(false);setTab("orders");setToast("طلبك اتسجل بنجاح 🎉")}
 async function saveAddress(){if(!session)return openLogin();if(!newAddress.address_line.trim())return setToast("اكتب العنوان");const first=addresses.length===0;const {data,error}=await supabase.from("addresses").insert({user_id:session.user.id,label:newAddress.label.trim()||"العنوان",address_line:newAddress.address_line.trim(),is_default:first}).select().single();if(error)return setToast(error.message);setAddresses(xs=>[...xs,data]);setAddressId(data.id);setNewAddress({label:"البيت",address_line:""});setToast("العنوان اتضاف ✓")}
 async function defaultAddress(a:Address){if(!session)return;const {error:e1}=await supabase.from("addresses").update({is_default:false}).eq("user_id",session.user.id);if(e1)return setToast(e1.message);const {error}=await supabase.from("addresses").update({is_default:true}).eq("id",a.id).eq("user_id",session.user.id);if(error)return setToast(error.message);setAddresses(xs=>xs.map(x=>({...x,is_default:x.id===a.id})));setAddressId(a.id)}
 async function fav(id:string){if(!session)return openLogin();if(favorites.includes(id)){const {error}=await supabase.from("favorites").delete().eq("user_id",session.user.id).eq("store_id",id);if(error)return setToast(error.message);setFavorites(xs=>xs.filter(x=>x!==id))}else{const {error}=await supabase.from("favorites").insert({user_id:session.user.id,store_id:id});if(error)return setToast(error.message);setFavorites(xs=>[...xs,id])}}
 async function cancel(o:Order){const {error}=await supabase.rpc("customer_cancel_order",{p_order_id:o.id});if(error)return setToast(error.message);setOrders(xs=>xs.map(x=>x.id===o.id?{...x,status:"cancelled"}:x));setToast("الطلب اتلغى")}
 async function sendReview(){if(!reviewing)return;const {error}=await supabase.rpc("submit_store_review",{p_order_id:reviewing.id,p_rating:rating,p_comment:comment});if(error)return setToast(error.message);setReviewing(null);setRating(5);setComment("");setToast("شكرًا على تقييمك ❤️")}
 async function submitApplication(){if(!session)return openSignup();if(!joinMode)return;const category=applicationForm.category==="أخرى"?applicationForm.custom_category.trim():applicationForm.category;if(joinMode==="merchant"&&(!applicationForm.business_name.trim()||!applicationForm.address.trim()||!category))return setToast("اكمل بيانات النشاط");const result=joinMode==="merchant"?await supabase.rpc("apply_as_merchant",{p_business_name:applicationForm.business_name.trim(),p_phone:applicationForm.phone,p_address:applicationForm.address.trim(),p_category:category}):await supabase.rpc("apply_as_driver",{p_full_name:applicationForm.full_name.trim()||profile?.full_name||"مستخدم طلبك",p_phone:applicationForm.phone,p_vehicle_type:applicationForm.vehicle_type});if(result.error)return setToast(result.error.message);setJoinMode(null);setToast("طلبك اتبعت للإدارة للمراجعة ✓")}
 const filtered=useMemo(()=>stores.filter(s=>(cat==="الكل"||s.category===cat)&&(!query||`${s.name} ${s.description} ${s.category}`.toLowerCase().includes(query.toLowerCase()))),[stores,cat,query]);
 const menu=active?items.filter(i=>i.store_id===active.id):[];const cartStore=cart[0]?stores.find(s=>s.id===cart[0].item.store_id):null;const subtotal=cart.reduce((a,l)=>a+l.qty*Number(l.item.price),0);const delivery=Number(cartStore?.delivery_fee||0);const total=subtotal+delivery;const count=cart.reduce((a,l)=>a+l.qty,0);const selectedAddressId=addressId||addresses.find(a=>a.is_default)?.id||null;const unread=notifications.filter(n=>!n.is_read).length;
 if(boot)return <div className="boot">جاري تشغيل طلبك…</div>;
 return <div className="app" dir="rtl"><header><div className="brand" onClick={()=>setTab("home")}><span className="logo">ط</span><div><b>طلبك</b><small>دليفري</small></div></div><div className="actions"><button onClick={()=>setTab("profile")}>⌖</button><button onClick={()=>setTab("profile")}>🔔{unread>0&&<i>{unread}</i>}</button><button className="avatar" onClick={()=>setTab("profile")}>{(profile?.full_name||"ز").slice(0,1)}</button></div></header>
 {tab==="home"&&<main className="page"><section className="hero"><div><span className="kicker lime">طلبك في طريقه ليك</span><h1>الأكل، البيت، <em>وأي حاجة</em>… في طلب واحد.</h1><p>اكتشف اللي حواليك، اختار طلبك، وخلّي علينا باقي الرحلة.</p><div className="search"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="مطعم، أكلة، منتج…"/>{query&&<button onClick={()=>setQuery("")}>×</button>}</div><div className="trust"><span>⚡ سريع</span><span>🛡️ موثوق</span><span>💵 كاش</span></div></div><div className="hero-art"><div className="glow one"/><div className="glow two"/><div className="promo"><small>🔥 الأكثر طلبًا</small><b>سماش برجر + بطاطس</b><span>من 145 ج.م • تحضير سريع</span><div><i/></div></div></div></section><section className="section"><div className="section-head"><div><span className="kicker">استكشف</span><h2>إيه نفسك فيه؟</h2></div></div><div className="chips">{CATS.map(c=><button className={cat===c?"active":""} key={c} onClick={()=>setCat(c)}><span>{IC[c]}</span>{c}</button>)}</div></section><section className="section"><div className="section-head"><div><span className="kicker">مختارات تستاهل</span><h2>{cat==="الكل"?"أماكن تستاهل التجربة":cat}</h2></div><small>{filtered.length} مكان</small></div><div className="grid">{filtered.map(s=><article className="store" key={s.id} onClick={()=>setActive(s)}><div className="media">{s.image_url?<img src={s.image_url} alt=""/>:<div className="fallback">{s.name[0]}</div>}<button className={`heart ${favorites.includes(s.id)?"on":""}`} onClick={e=>{e.stopPropagation();void fav(s.id)}}>{favorites.includes(s.id)?"♥":"♡"}</button><span>{s.is_open?"مفتوح":"مغلق"}</span></div><div className="store-copy"><div><h3>{s.name}</h3><b>★ {Number(s.rating||0).toFixed(1)}</b></div><p>{s.description}</p><small>{s.category} <i>•</i> {s.prep_minutes+20}–{s.prep_minutes+30} د <i>•</i> {money(s.delivery_fee)} توصيل</small></div></article>)}</div></section><section className="partner"><div><span className="kicker">لأصحاب المتاجر والسائقين</span><h2>كبر شغلك مع طلبك.</h2><p>قدّم دلوقتي كمتجر أو سائق، وفريق طلبك يراجع الطلب.</p><div className="partner-actions"><button onClick={()=>{if(!session)return openSignup();setJoinMode("merchant")}}>أنا متجر</button><button className="ghost" onClick={()=>{if(!session)return openSignup();setJoinMode("driver")}}>أنا سائق</button></div></div><div className="numbers"><div><b>24/7</b><span>تشغيل</span></div><div><b>مكان واحد</b><span>لكل الطلبات</span></div></div></section></main>}
 {tab==="orders"&&<main className="page inner"><div className="title"><div><span className="kicker">رحلة طلباتك</span><h1>طلباتي</h1></div><button className="outline" onClick={()=>setTab("home")}>+ طلب جديد</button></div>{!session?<div className="login-cta"><div><b>سجّل دخول عشان تحفظ الرحلات</b><p>طلباتك ومفضلاتك وعناوينك في مكان واحد.</p></div><button onClick={openLogin}>تسجيل الدخول</button></div>:orders.length?orders.map(o=><article className="order" key={o.id}><div className="order-head"><div><small>#{o.id.slice(0,6).toUpperCase()} • {ago(o.created_at)}</small><h3>{o.stores?.name||"متجر"}</h3></div><span>{ST[o.status]||o.status}</span></div><div className="steps">{["استلام","تجهيز","سائق","طريق","تم"].map((x,i)=><div className={i*2<=["pending","accepted","preparing","ready","assigned","picked_up","on_the_way","delivered"].indexOf(o.status)?"done":""} key={x}><i>{i===4?"✓":"•"}</i><small>{x}</small></div>)}</div><div className="order-foot"><span>{typeof o.delivery_address==="string"?o.delivery_address:"العنوان محفوظ في الطلب"}</span><b>{money(Number(o.total))}</b><button onClick={()=>setOrderView(o)}>تفاصيل</button>{["pending","accepted"].includes(o.status)&&<button className="danger-link" onClick={()=>void cancel(o)}>إلغاء</button>}{o.status==="delivered"&&<button onClick={()=>setReviewing(o)}>قيّم</button>}</div></article>):<div className="empty"><div>🛍️</div><h3>لسه مفيش طلبات</h3><p>ابدأ أول طلب وهتلاقي الرحلة كلها هنا.</p><button onClick={()=>setTab("home")}>ابدأ دلوقتي</button></div>}</main>}
 {tab==="saved"&&<main className="page inner"><div className="title"><div><span className="kicker">محفوظاتك</span><h1>المفضلة</h1></div></div>{favorites.length?<div className="grid">{stores.filter(s=>favorites.includes(s.id)).map(s=><article className="store" key={s.id} onClick={()=>setActive(s)}><div className="media">{s.image_url&&<img src={s.image_url} alt=""/>}<button className="heart on" onClick={e=>{e.stopPropagation();void fav(s.id)}}>♥</button></div><div className="store-copy"><div><h3>{s.name}</h3><b>★ {Number(s.rating||0).toFixed(1)}</b></div><p>{s.description}</p><small>{s.category} <i>•</i> {money(s.delivery_fee)} توصيل</small></div></article>)}</div>:<div className="empty"><div>♡</div><h3>المفضلة فاضية</h3><p>اضغط القلب على أي متجر عشان تحفظه.</p><button onClick={()=>setTab("home")}>استكشف</button></div>}</main>}
 {tab==="profile"&&<main className="page inner"><section className="profile"><div className="pavatar">{(profile?.full_name||"ز").slice(0,1)}</div><div><span className="kicker lime">حسابك</span><h1>{session?(profile?.full_name||"مستخدم طلبك"):"ضيف"}</h1><p>{session?(profile?.phone||"رقم الموبايل محفوظ"):"سجّل الدخول عشان كل المميزات"}</p></div>{session?<button onClick={()=>{void supabase.auth.signOut()}}>خروج</button>:<button onClick={openLogin}>تسجيل الدخول</button>}</section><section className="notifications"><div className="section-head"><div><span className="kicker">تنبيهاتك</span><h2>الإشعارات</h2></div></div>{notifications.length?notifications.slice(0,6).map(n=><div className={`note ${n.is_read?"":"new"}`} key={n.id}><span>●</span><div><b>{n.title}</b><p>{n.body}</p><small>{ago(n.created_at)}</small></div></div>):<div className="muted">مفيش إشعارات لحد دلوقتي.</div>}</section><section className="section"><div className="section-head"><div><span className="kicker">التوصيل</span><h2>عناويني</h2></div></div><div className="address-list">{addresses.map(a=><div className={`address-card ${a.is_default?"default":""}`} key={a.id}><div><b>{a.label}</b><p>{a.address_line}</p>{a.is_default&&<small>الافتراضي</small>}</div>{!a.is_default&&<div className="address-actions"><button onClick={()=>void defaultAddress(a)}>اختيار</button></div>}</div>)}<div className="add-address"><input value={newAddress.label} onChange={e=>setNewAddress({...newAddress,label:e.target.value})} placeholder="اسم العنوان"/><input value={newAddress.address_line} onChange={e=>setNewAddress({...newAddress,address_line:e.target.value})} placeholder="العنوان بالتفصيل"/><button className="primary" onClick={()=>void saveAddress()}>+ إضافة عنوان</button></div></div></section><section className="section"><div className="section-head"><div><span className="kicker">اشتغل معانا</span><h2>انضم لطلبك</h2></div></div><div className="join-grid"><button onClick={()=>{if(!session)return openSignup();setJoinMode("merchant")}}><span>🏪</span><b>انضم كتاجر</b><small>اعرض منتجاتك واستقبل الطلبات</small></button><button onClick={()=>{if(!session)return openSignup();setJoinMode("driver")}}><span>🛵</span><b>اشتغل كسائق</b><small>استقبل رحلات من التطبيق</small></button></div></section></main>}
 <nav className="nav"><button className={tab==="home"?"active":""} onClick={()=>setTab("home")}><b>⌂</b>الرئيسية</button><button className={tab==="orders"?"active":""} onClick={()=>setTab("orders")}><b>▣</b>طلباتي</button><button className="cart" onClick={()=>setCartOpen(true)}>🛒{count>0&&<i>{count}</i>}</button><button className={tab==="saved"?"active":""} onClick={()=>setTab("saved")}><b>♡</b>المفضلة</button><button className={tab==="profile"?"active":""} onClick={()=>setTab("profile")}><b>◯</b>حسابي</button></nav>
 {active&&<div className="shade" onClick={()=>setActive(null)}><div className="store-modal" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setActive(null)}>×</button><div className="cover">{active.image_url&&<img src={active.image_url} alt=""/>}</div><div className="modal-body"><div className="modal-title"><div><span className="open">{active.is_open?"مفتوح الآن":"مغلق الآن"}</span><h2>{active.name}</h2><p>{active.description}</p></div><button className={`heart big ${favorites.includes(active.id)?"on":""}`} onClick={()=>void fav(active.id)}>{favorites.includes(active.id)?"♥":"♡"}</button></div><div className="meta">★ {Number(active.rating||0).toFixed(1)} <i>•</i> {active.prep_minutes+20}–{active.prep_minutes+30} دقيقة <i>•</i> {money(active.delivery_fee)} توصيل</div><div className="menu">{menu.map(it=><div className="item" key={it.id}><div className="item-img">{it.image_url&&<img src={it.image_url} alt=""/>}</div><div><b>{it.name}</b><p>{it.description}</p><strong>{money(Number(it.price))}</strong></div><button onClick={()=>addItem(it)}>+</button></div>)}</div></div></div></div>}
 {cartOpen&&<div className="shade" onClick={()=>setCartOpen(false)}><aside className="drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><div><span className="kicker">جاهز؟</span><h2>سلتك</h2></div><button className="x" onClick={()=>setCartOpen(false)}>×</button></div>{cart.length?<><div className="cart-store"><b>{cartStore?.name}</b><small>{count} منتج</small></div>{cart.map(l=><div className="line" key={l.item.id}><img src={l.item.image_url||""} alt=""/><div><b>{l.item.name}</b><small>{money(Number(l.item.price))}</small></div><div className="counter"><button onClick={()=>qty(l.item.id,-1)}>−</button><b>{l.qty}</b><button onClick={()=>qty(l.item.id,1)}>+</button></div></div>)}<div className="sum"><div><span>الإجمالي الفرعي</span><b>{money(subtotal)}</b></div><div><span>التوصيل</span><b>{money(delivery)}</b></div><div className="grand"><span>الإجمالي</span><b>{money(total)}</b></div></div><button className="primary full" onClick={()=>setCheckout(true)}>إتمام الطلب</button></>:<div className="empty"><div>🛒</div><h3>السلة فاضية</h3><p>ضيف أول منتج عشان تبدأ.</p></div>}{checkout&&<div className="checkout"><div className="drawer-head"><div><span className="kicker">آخر خطوة</span><h2>تأكيد الطلب</h2></div><button className="x" onClick={()=>setCheckout(false)}>×</button></div>{session?(addresses.length?<div className="checkout-addresses">{addresses.map(a=><button className={selectedAddressId===a.id?"selected":""} key={a.id} onClick={()=>setAddressId(a.id)}><b>{a.label}</b><span>{a.address_line}</span></button>)}</div>:<div className="login-cta"><div><b>مفيش عنوان محفوظ</b><p>أضف عنوان من حسابك.</p></div><button onClick={()=>{setCheckout(false);setCartOpen(false);setTab("profile")}}>إضافة</button></div>):<div className="login-cta"><div><b>سجّل دخول الأول</b><p>لازم حساب علشان نكمل الطلب.</p></div><button onClick={openLogin}>تسجيل الدخول</button></div>}<label>ملاحظات<textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="الدور، علامة مميزة، تعليمات للسائق..."/></label><div className="payments"><button className={payment==="cash"?"selected":""} onClick={()=>setPayment("cash")}>💵<b>كاش</b><small>عند الاستلام</small></button><button className={payment==="online"?"selected":""} onClick={()=>setPayment("online")}>💳<b>إلكتروني</b><small>جاهز للربط</small></button></div><button className="primary full" onClick={()=>void place()}>تأكيد الطلب • {money(total)}</button></div>}</aside></div>}
 {orderView&&<div className="shade" onClick={()=>setOrderView(null)}><div className="order-detail" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setOrderView(null)}>×</button><span className="kicker">متابعة الطلب</span><h2>{orderView.stores?.name||"طلبك"}</h2><div className="detail-status"><b>{ST[orderView.status]||orderView.status}</b><small>آخر تحديث {ago(orderView.updated_at)}</small></div><div className="detail-total"><span>الإجمالي</span><b>{money(Number(orderView.total))}</b></div><div className="detail-row"><span>العنوان</span><b>{typeof orderView.delivery_address==="string"?orderView.delivery_address:"العنوان محفوظ في الطلب"}</b></div><div className="detail-row"><span>الدفع</span><b>{orderView.payment_method==="cash"?"كاش":"إلكتروني"}</b></div>{orderView.estimated_minutes&&<div className="eta">⏱️ متوقع خلال حوالي {orderView.estimated_minutes} دقيقة</div>}</div></div>}
 {reviewing&&<div className="shade" onClick={()=>setReviewing(null)}><div className="auth" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setReviewing(null)}>×</button><span className="kicker">رأيك يفرق</span><h2>قيّم تجربتك</h2><div className="stars">{[1,2,3,4,5].map(n=><button className={n<=rating?"on":""} key={n} onClick={()=>setRating(n)}>★</button>)}</div><textarea className="review-box" value={comment} onChange={e=>setComment(e.target.value)} placeholder="قول لنا رأيك..."/><button className="primary full" onClick={()=>void sendReview()}>إرسال التقييم</button></div></div>}
 {joinMode&&<div className="shade" onClick={()=>setJoinMode(null)}><div className="auth" onClick={e=>e.stopPropagation()}><button className="x" onClick={()=>setJoinMode(null)}>×</button><span className="kicker">انضم لطلبك</span><h2>{joinMode==="merchant"?"طلب انضمام متجر":"طلب انضمام سائق"}</h2>{joinMode==="merchant"?<><input value={applicationForm.business_name} onChange={e=>setApplicationForm({...applicationForm,business_name:e.target.value})} placeholder="اسم النشاط"/><input value={applicationForm.phone} onChange={e=>setApplicationForm({...applicationForm,phone:e.target.value})} placeholder="رقم الموبايل (اختياري لو محفوظ)"/><input value={applicationForm.address} onChange={e=>setApplicationForm({...applicationForm,address:e.target.value})} placeholder="عنوان النشاط"/><select value={applicationForm.category} onChange={e=>setApplicationForm({...applicationForm,category:e.target.value})}><option>مطاعم</option><option>بقالة</option><option>مخبوزات</option><option>صيدلية</option><option>مشروبات</option><option>حلويات</option><option>عناية</option><option>خدمات</option><option>أخرى</option></select>{applicationForm.category==="أخرى"&&<input value={applicationForm.custom_category} onChange={e=>setApplicationForm({...applicationForm,custom_category:e.target.value})} placeholder="اكتب نوع النشاط"/>}</>:<><input value={applicationForm.full_name} onChange={e=>setApplicationForm({...applicationForm,full_name:e.target.value})} placeholder="الاسم بالكامل"/><input value={applicationForm.phone} onChange={e=>setApplicationForm({...applicationForm,phone:e.target.value})} placeholder="رقم الموبايل (اختياري لو محفوظ)"/><select value={applicationForm.vehicle_type} onChange={e=>setApplicationForm({...applicationForm,vehicle_type:e.target.value})}><option>سكوتر</option><option>موتسيكل</option><option>عجلة</option></select></>}<button className="primary full" onClick={()=>void submitApplication()}>{session?"إرسال الطلب":"إنشاء حساب ثم التقديم"}</button></div></div>}
 {toast&&<div className="toast">{toast}</div>}</div>;
}