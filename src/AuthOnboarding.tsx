import { useMemo, useState } from "react";
import { supabase } from "./lib/supabase";
import "./auth.css";

type Mode = "login" | "signup";
type Join = "customer" | "merchant" | "driver";
type Form = { fullName:string; phone:string; password:string; confirm:string; businessName:string; category:string; customCategory:string; address:string; vehicleType:string };
const initial:Form={fullName:"",phone:"",password:"",confirm:"",businessName:"",category:"مطاعم",customCategory:"",address:"",vehicleType:"موتسيكل"};
const normalizePhone=(v:string)=>{const digits=v.replace(/[٠-٩]/g,d=>String("٠١٢٣٤٥٦٧٨٩".indexOf(d))).replace(/\D/g,"");if(digits.startsWith("20")&&digits.length===12)return `+${digits}`;if(digits.startsWith("01")&&digits.length===11)return `+20${digits.slice(1)}`;if(digits.startsWith("1")&&digits.length===10)return `+20${digits}`;return `+${digits}`};
// Phone-only UI with an internal Auth identity. No email field, SMS, or OTP is required.
const authEmail=(phone:string)=>`${phone.replace(/\D/g,"")}@talabak.app`;
const readableError=(message:string)=>{const m=message.toLowerCase();if(m.includes("invalid login credentials"))return "رقم الموبايل أو كلمة المرور غير صحيحة.";if(m.includes("user already registered")||m.includes("already registered"))return "الرقم ده مسجل بالفعل.";if(m.includes("email not confirmed"))return "الحساب محتاج تفعيل. تواصل مع الإدارة.";if(m.includes("password"))return "كلمة المرور لازم تكون 6 أحرف على الأقل.";if(m.includes("duplicate")||m.includes("unique"))return "الرقم ده مرتبط بحساب موجود بالفعل. استخدم نفس الحساب لإضافة دور جديد.";if(m.includes("rate limit"))return "استنى شوية وحاول تاني.";return message||"حصلت مشكلة. جرّب تاني."};
export default function AuthOnboarding(){
 const [mode,setMode]=useState<Mode>("login"),[join,setJoin]=useState<Join>("customer"),[form,setForm]=useState<Form>(initial),[busy,setBusy]=useState(false),[message,setMessage]=useState<{kind:"error"|"success";text:string}|null>(null),[showPassword,setShowPassword]=useState(false);
 const roleCopy=useMemo(()=>({customer:{icon:"🛍️",title:"عميل",desc:"اطلب أكل، بقالة، مخبوزات وخدمات من مكان واحد."},merchant:{icon:"🏪",title:"تاجر",desc:"قدّم نشاطك واستقبل الطلبات بعد اعتماد الإدارة."},driver:{icon:"🛵",title:"سائق",desc:"اشتغل بمرونة واستقبل الطلبات القريبة منك."}}),[]);
 function set<K extends keyof Form>(key:K,value:Form[K]){setForm(x=>({...x,[key]:value}));setMessage(null)}
 function chooseRole(r:Join){setJoin(r);setMode("signup");setMessage(null)}
 async function finishJoin(p:any){if(!p||p.join==="customer")return;if(p.join==="merchant"){const {error}=await supabase.rpc("apply_as_merchant",{p_business_name:String(p.businessName||""),p_phone:String(p.phone||""),p_address:String(p.address||""),p_category:String(p.category||"مطاعم")});if(error)throw new Error(readableError(error.message))}else{const {error}=await supabase.rpc("apply_as_driver",{p_full_name:String(p.fullName||""),p_phone:String(p.phone||""),p_vehicle_type:String(p.vehicleType||"موتسيكل")});if(error)throw new Error(readableError(error.message))}localStorage.removeItem("talabak_pending_join")}
 async function submit(){setMessage(null);const phone=normalizePhone(form.phone);if(!/^\+20\d{10}$/.test(phone))return setMessage({kind:"error",text:"اكتب رقم موبايل مصري صحيح، مثال: 01012345678"});if(form.password.length<6)return setMessage({kind:"error",text:"كلمة المرور لازم تكون 6 أحرف على الأقل."});if(mode==="signup"){if(!form.fullName.trim())return setMessage({kind:"error",text:"اكتب الاسم بالكامل."});if(form.password!==form.confirm)return setMessage({kind:"error",text:"كلمتا المرور مش متطابقين."});if(join==="merchant"&&(!form.businessName.trim()||!form.address.trim()))return setMessage({kind:"error",text:"للتاجر لازم اسم النشاط والعنوان."});if(join==="merchant"&&form.category==="أخرى"&&!form.customCategory.trim())return setMessage({kind:"error",text:"اكتب نوع النشاط في خانة أخرى."})}setBusy(true);try{const email=authEmail(phone);if(mode==="login"){const {error}=await supabase.auth.signInWithPassword({email,password:form.password});if(error)throw new Error(readableError(error.message));window.location.reload();return}const pending={join,fullName:form.fullName.trim(),phone,businessName:form.businessName.trim(),address:form.address.trim(),category:form.category==="أخرى"?form.customCategory.trim():form.category,vehicleType:form.vehicleType};localStorage.setItem("talabak_pending_join",JSON.stringify(pending));const {data,error}=await supabase.auth.signUp({email,password:form.password,options:{data:{full_name:form.fullName.trim(),phone}}});if(error){if(/already registered|already exists|user already/i.test(error.message)){const {error:loginError}=await supabase.auth.signInWithPassword({email,password:form.password});if(loginError)throw new Error("الرقم ده عنده حساب بالفعل. استخدم كلمة مرور الحساب الحالية لإضافة الفئة المطلوبة.");await finishJoin(pending);setMessage({kind:"success",text:join==="customer"?"تم الدخول للحساب الموجود بنجاح 🎉":join==="merchant"?"تم ربط طلب التاجر بنفس الحساب وإرساله للإدارة للمراجعة.":"تم ربط طلب السائق بنفس الحساب وإرساله للإدارة للمراجعة."});window.setTimeout(()=>window.location.reload(),700);return}throw new Error(readableError(error.message))}if(!data.session)throw new Error("تأكيد البريد مفعّل في Supabase. لازم يكون Email Confirmations متوقف لحسابات طلبك الداخلية.");await finishJoin(pending);setMessage({kind:"success",text:join==="customer"?"الحساب اتعمل بنجاح 🎉":join==="merchant"?"طلب التاجر اتبعت للإدارة للمراجعة.":"طلب السائق اتبعت للإدارة للمراجعة."});window.setTimeout(()=>window.location.reload(),700)}catch(e:any){setMessage({kind:"error",text:e?.message||"حصلت مشكلة."})}finally{setBusy(false)}}
 return <div className="auth-page" dir="rtl"><div className="auth-shell"><aside className="auth-side"><div className="brand"><span>ط</span><div><b>طلبك</b><small>دليفري لكل احتياجاتك</small></div></div><div className="hero-copy"><span className="eyebrow">حساب واحد • أدوار متعددة • تجربة واحدة</span><h1>اطلب بسهولة،<br/>اشتغل بذكاء،<br/><em>وكبّر شغلك.</em></h1><p>رقم موبايل واحد = حساب واحد. نفس الحساب يقدر يجمع عميل + سائق + تاجر حسب الأدوار المعتمدة.</p></div><div className="side-points"><div>✓ دخول وتسجيل من خلال رقم الموبايل</div><div>✓ نفس الرقم = نفس الحساب</div><div>✓ تقدر تضيف أكتر من دور لنفس الحساب</div></div></aside><main className="auth-main"><div className="auth-top"><button className="back-btn" onClick={()=>mode==="signup"?(setMode("login"),setJoin("customer"),setMessage(null)):window.location.href="/"}>← الرئيسية</button><span>حسابك على طلبك</span></div>{mode==="signup"&&<section className="role-picker"><div className="section-head"><div><span className="mini">اختار طريقك</span><h2>إنت عايز تضيف إيه لحسابك؟</h2></div><small>لو الرقم مسجل قبل كده، هنستخدم نفس الحساب ونضيف الدور المطلوب بدل ما نعمل حساب جديد.</small></div><div className="role-grid">{(Object.keys(roleCopy) as Join[]).map(r=><button key={r} className={`role-card ${join===r?"selected":""}`} onClick={()=>chooseRole(r)}><span className="role-icon">{roleCopy[r].icon}</span><span><b>{roleCopy[r].title}</b><small>{roleCopy[r].desc}</small></span><i>›</i></button>)}</div></section>}<section className="auth-card"><div className="auth-card-head"><span className="mini">{mode==="login"?"دخول آمن":"حساب جديد / إضافة دور"}</span><h2>{mode==="login"?"أهلاً بيك في طلبك":"ابدأ رحلتك مع طلبك"}</h2><p>{mode==="login"?"ادخل برقم الموبايل وكلمة المرور علشان تكمل.":join==="customer"?"بياناتك الأساسية كفاية علشان تبدأ كعميل.":join==="merchant"?"خلّي نشاطك جاهز للانضمام واستقبال الطلبات.":"قدّم بياناتك الأساسية وابدأ إجراءات الانضمام كسائق."}</p></div>{message&&<div className={`auth-alert ${message.kind}`}>{message.text}</div>}<div className="form-grid">{mode==="signup"&&<label><span>الاسم بالكامل</span><input value={form.fullName} onChange={e=>set("fullName",e.target.value)} placeholder="مثال: عبدالله عمرو" autoComplete="name"/></label>}<label className={mode==="signup"?"full":""}><span>رقم الموبايل</span><input value={form.phone} onChange={e=>set("phone",e.target.value)} placeholder="01012345678" inputMode="tel" autoComplete="tel" dir="ltr"/></label><label><span>كلمة المرور</span><div className="password-wrap"><input type={showPassword?"text":"password"} value={form.password} onChange={e=>set("password",e.target.value)} placeholder="6 أحرف على الأقل" autoComplete={mode==="login"?"current-password":"new-password"} dir="ltr"/><button type="button" onClick={()=>setShowPassword(v=>!v)}>{showPassword?"إخفاء":"إظهار"}</button></div></label>{mode==="signup"&&<label><span>تأكيد كلمة المرور</span><input type="password" value={form.confirm} onChange={e=>set("confirm",e.target.value)} placeholder="كرر كلمة المرور" autoComplete="new-password" dir="ltr"/></label>}{mode==="signup"&&join==="merchant"&&<><label><span>اسم النشاط / المتجر</span><input value={form.businessName} onChange={e=>set("businessName",e.target.value)} placeholder="مثال: مطعم وكافيه النيل"/></label><label><span>نوع النشاط</span><select value={form.category} onChange={e=>set("category",e.target.value)}><option>مطاعم</option><option>بقالة</option><option>مخبوزات</option><option>صيدلية</option><option>مشروبات</option><option>حلويات</option><option>عناية</option><option>خدمات</option><option>أخرى</option></select></label>{form.category==="أخرى"&&<label className="full"><span>اكتب نوع النشاط</span><input value={form.customCategory} onChange={e=>set("customCategory",e.target.value)} placeholder="مثال: محل ورد، قطع غيار، مغسلة..."/></label>}<label className="full"><span>عنوان النشاط</span><input value={form.address} onChange={e=>set("address",e.target.value)} placeholder="المنطقة، الشارع، رقم المحل"/></label><div className="form-info full">بيانات النشاط تدخل للمراجعة قبل التفعيل، والنوع المخصص في «أخرى» يتحفظ كما كتبته.</div></>}{mode==="signup"&&join==="driver"&&<><label><span>نوع المركبة</span><select value={form.vehicleType} onChange={e=>set("vehicleType",e.target.value)}><option>سكوتر</option><option>موتسيكل</option><option>عجلة</option></select></label><div className="driver-note"><b>المركبات المتاحة</b><span>سكوتر • موتسيكل • عجلة فقط. مفيش سيارة أو «أخرى».</span></div><div className="form-info full">بعد التقديم، الإدارة تراجع الطلب قبل تفعيل استقبال الطلبات.</div></>}</div><button className="primary-btn" disabled={busy} onClick={()=>void submit()}>{busy?"جاري المعالجة…":mode==="login"?"تسجيل الدخول بالرقم ←":join==="customer"?"إنشاء / دخول كعميل ←":join==="merchant"?"إنشاء / إضافة تاجر ←":"إنشاء / إضافة سائق ←"}</button></section></main></div></div>;
}